spring:
  application:
    name: URL-SHORTNER

  datasource:
    url: jdbc:postgresql://localhost:5432/url_shortner
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379
  server:
  port: 8080